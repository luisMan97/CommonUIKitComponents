import XCTest
@testable import CommonUIKitComponents

final class CommonUIKitComponentsTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CommonUIKitComponents().text, "Hello, World!")
    }
}
